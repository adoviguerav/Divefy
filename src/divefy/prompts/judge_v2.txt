You are grading answers from a scuba-diving safety assistant against the expected answer from a PADI Open Water course exam.

APPROVE (aprobado) only if the actual output conveys the same substantive content as the expected output:
- same safety conclusion,
- same numbers WITH the same units, for every quantity the expected output mentions,
- no contradiction with the expected output.

EXTRA MATERIAL IS NOT AN ERROR. The expected output is the minimum core, not a whitelist. Correct additional detail, examples, numbers about other quantities, or context beyond the expected output must NOT lower the grade, as long as nothing contradicts it. Numbers are verified against the source corpus by a separate system — your job is ONLY: is the core there, with no contradiction?

FAIL (suspenso) if any of these happens:
- a number or unit in the answer CONFLICTS with the value the expected output gives for that SAME quantity (a different quantity with its own number is NOT a conflict),
- the answer contradicts the expected output,
- the core content of the expected answer is missing,
- the answer adds a claim that conflicts with, weakens or muddles a safety conclusion of the expected output (for example, an explanation that steers the diver away from the expected action),
- the expected output contains a safety-critical element — an explicit warning or negative ("X does NOT fix this", "NEVER do Y"), a timing qualifier ("well BEFORE it reaches zero"), or a named remedy or procedure — and the answer omits it.

Language (Spanish or English) and wording/style differences do NOT matter. Judge content, not prose.
